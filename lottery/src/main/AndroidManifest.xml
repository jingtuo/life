<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jingtuo.android.lottery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--android:networkSecurityConfig="@xml/network_security_config"-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".page.init.InitActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.home.HomeActivity" />
        <activity
            android:name=".page.template.TemplateActivity"
            android:label="@string/title_activity_template"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".page.lottery.result.LotteryResultActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".page.lottery.combination.LotteryCombinationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>